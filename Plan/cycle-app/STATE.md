# Cycle App — state

**Hard cap: 60 lines. One line per item.** Forensic detail goes to `Docs/Archive/Cycle App Log.md`, never here.

**On "Wrap up", run `README.md`'s "End every session with" checklist.** STATE.md is always loaded at session start, the README isn't — this line is the only way a session learns what "wrap up" means.

**Last reviewed commit:** HEAD (reviewed by ca9a). Unreviewed tally: ca10 (~150 lines, unreviewed). Next slice is **ca11** (a refusal Google invents after the write has already landed — found while running `verify.bat` for ca10, reproduced on demand), then **ca8**, the final review over `1065c1e...HEAD`.
## Done
- **ca1** — 4 defects fixed in `index.html` + `deploy.bat`, headless 2026-08-26, live 2026-09-13.
- **ca2 + ca2a** — `Tools/migrate-sheet.js` (sheet ID is a CLI arg). 177 rows → 165, no Temp invented. `Time` was silently dropped, then recovered from Mike's xlsx export (88 values, the only loss); the script now carries it and refuses unmapped columns.
- **ca3 + ca3a + ca3b** — proxy live 2026-09-13: both roles read, bad tokens rejected, old sheet refused; `deploy.bat` was silently skipping the push — fixed. Reviews of `1065c1e..HEAD` and `3ee13e2..HEAD`: `hasData`, strict `=== 'TRUE'` reads, a double sheet open, all 5 later targets resolved. Both PASS, ca3b live 2026-09-14.
- **ca7 + ca7a** — pulled forward ahead of ca4. Display cache `cycleCache` (dropped on a `PROXY_URL` change), dismissible dated banner carrying ca3a's warnings, 3-day "Out of date" expiry, 20s timeout that self-reloads once past the browser cache. New `Tools/staleness-selfcheck.js`. ca7a reviewed `24f6067..HEAD`: targets 1/2/5 clean, 3 fixed — an unsaved token scrubbed from the address bar and then lost on reload; a full store blocking a new link (now drops the cache and retries); an unbounded warning banner (capped at 4 + a count). 8 checks added, all self-checks PASS. Private-window reload confirmed by Mike 2026-09-15. **Closed 2026-09-15 on Mike's phone** (reader link swapped in: no tab bar, no entry form, dated dashboard cold in aeroplane mode). Her device is a caveat, not an outstanding test.
- **ca4 + ca4a** — safety engine in one marked `SAFETY` block: opening bleed run, three-over-six in integer hundredths, window opens on the later of marker+4 and the fire day. ca3's synthesised `Cycle` column deleted, readers on `Flow`/`Ovulation`. Missed-Day-1 guard reversed (see slice file). New `Tools/safety-selfcheck.js`; `verify-proxy` reproduced d24/d22/d30/d37/d27 on the real data 2026-09-15. ca4a reviewed `4050005..HEAD` and found ca4 had shipped a **blank dashboard**: `render()` still read `ovDay`, a const ca4 had just deleted — a `ReferenceError` before `#app` was written, and every self-check passed anyway because none ran `render()`. Fixed, plus ca3's Ovulation-and-bleeding warning restored. New `Tools/render-selfcheck.js` runs the whole page script under stub browser globals; mutation-tested. All PASS, live and confirmed by Mike 2026-09-15.
- **ca5** — writer-only Log tab: catch-up list newest first, one card per day, full ca2 form, Flow/Quality/Position/Exclude added to the log table. `doPost` in `Code.gs` writes one row per date — writer token checked server-side, update-or-insert **in date order** under a script lock, so a retry after Google's flaky redirect cannot duplicate. Only changed fields are sent (migrated `cervix:` notes survive an edit); confirm step lists them; every failure shows "NOT saved — <reason>". Temp-less rows allowed (Mike, 2026-09-15: a missed reading is not a missed day). New `Tools/entry-selfcheck.js`; `verify-proxy` now proves reader-rejected / inserted-in-order / retry-does-not-duplicate / deleted — **All checks passed** on the real sheet 2026-09-15. POST CORS from Pages confirmed working.
- **ca5a** — review of `2a7352f..db5a8ee`. Targets 1/2/5 cleared, 8 is Mike's eyes. 4 fixed: unticking a flag wrote a blank into a checkbox cell; a Note starting with `=` was stored as a formula and read back as its result; a non-ISO `Date` string would have inserted a new row at the top of the sheet; an open card paused every refresh silently and forever (now says so in the form after 30 min). Plus `buildLogRows` printed sheet text unescaped — a `<` in a Note ate the row. `render-selfcheck` now renders the **log** exit and an awkward migrated row; `verify-proxy` gained the checkbox and formula-note writes. All six self-checks PASS. Mike's live run confirmed the checkbox fix and found two: cycle 5's opening moved (data — `EXPECT.opensOn` updated), and a Note starting with `=` still became a formula. Apostrophe, plain-text format and `RichTextValue` all failed live — `SpreadsheetApp` has no literal write; text columns now go through the **advanced Sheets service** at `valueInputOption: RAW` (Mike must enable Services → Google Sheets API). `unreadableDates`/`textNotStored` were never read by the app — now shown on the saved line. Safe-window failure was new data, not a regression. Target 8 confirmed on the phone and fixed: Day and Date are `position: sticky` under 620px so the eight scrolling columns stay attached to a day. Sticky columns had a gap (auto layout ignores `width`; now min/max-width from one `--pin`, narrowed to 38px with tighter padding on Mike's word — the DAY header, not the digits, is the floor). The `RAW` write needed a `SpreadsheetApp.flush()` before it — the buffered write was landing last and putting the formula back. **Mike ran `verify-proxy` clean and passed the sticky columns on the phone. ca5a closed.**
- **ca6 + ca6a** — unsent write queue. A failed write is kept in its own key `cycleQueue` (never touched by ca7's quota recovery), one entry per date — merged, newest field wins, because `doPost` is update-or-insert per date and a second entry would silently replace the first. An entry leaves **only** on a confirmed `ok:true`, after which the sheet is re-read rather than trusted. A refusal (`read-only`/`no-access`/`not-configured`) is never queued; an already-queued one that gets refused stays, and says nothing more will send. Persistent, non-dismissible, writer-only banner with **Try sending now** / **Discard** (confirmed, the only other way out); uncapped, unlike ca7's 4-warning cap. Unreadable stored entries, a storage refusal, a partial flush and ca5a's `textNotStored` warnings all leave a line that survives a reload. Queued values are flagged on the day card, never merged into the row. Auto-flush on boot, on any successful read and on `online`; suppressed while a card is open (manual is allowed — it swaps the banner alone). New `Tools/queue-selfcheck.js` (pure + end-to-end) and `Tools/page-harness.js`, the stub browser `render-selfcheck` now shares — booting twice against one store is how a reload is tested. All seven self-checks PASS. **Live-confirmed by Mike 2026-09-15** (deployed in `9556f6a`). **ca6a** — review of `db5a8ee..HEAD`. Targets 1/3/6/7 clear. 4 fixed, all at a shared root: saving while a manual flush was in flight merged into the entry the flush then deleted, losing the edit silently (`saveEntry` now refuses while `_flushing`); a landed flush left `not sent yet` on the day cards with the contradicting banner gone (spans carry `data-iso` and are dropped, and a flush behind an open form says the list is older than the write); `escHTML` never escaped quotes, and `entryFormHTML` uses it inside `value="…"` — a migrated cell holding a `"` broke out; an unsent queue was invisible whenever `_role` was still `null` (writer, offline, cache dropped by `saveQueue` itself) — now gated on `=== 'reader'`, and `showMessage` carries the banner too. 5 checks added, each mutation-tested red; one was a false green first (node exited 0 on an unresolved await — third of these in this plan). All seven self-checks PASS. **Both closed 2026-09-15**: `verify.bat` ran clean end to end (every access, role, ca2-paste, safety and ca5 write-endpoint check), and the queue was exercised for real — a stuck entry flushed on a retry and the banner cleared on a confirmed `ok:true`.
- **ca9** — offline shell. New `sw.js`: the page is **network-first** (a cache-first worker is how `deploy.bat` reports success while the phone runs last week's safety engine), cache-first only for the version-pinned Chart.js, and pass-through for everything else — so `script.google.com` is never named and nothing from the sheet is ever in the worker's cache. `?v=` reloads from ca7's `selfRefresh()` are answered with `ignoreSearch` and stored without the query, so a rescue cannot grow the cache. Page side: registration guarded on `isSecureContext` and loud on failure; `chartsUnavailable()` draws "These charts need an internet connection" instead of throwing; new `cycleRole` key (kept out of `cycleCache`, which ca6/ca7 drop for quota) so a cold start with no signal still knows it is Mike — `showMessage()` now carries the tab bar and the full entry form, which is what makes ca6's queue reachable at all offline. A revoked token clears the remembered role, `_role` and `_tab`. Every `render(allCycles)` call site went through a new `redraw()`. New `Tools/offline-selfcheck.js` (17 checks under a stub worker); `render-selfcheck` gained 4 sections; `page-harness` gained `parentNode`. All 8 self-checks PASS; 11 mutations red, 3 of them false-green first. **Live on `f9ab1a7` and fully confirmed by Mike 2026-09-15**: cold start in aeroplane mode with the tab closed, the Log tab reached and a day queued from it, and the deploy-then-reopen check — a phone with a signal gets the new version on the next open.
- **ca9a** — review of `55db0c4..HEAD`. Targets 1/2/6 clear: no caller of `render()` survives outside `renderPayload()`, `entryScreenHTML([])` invents no day numbers and `entryProblem` needs no cycle data. 4 fixed, two of them at one root — **the remembered role outlived the token that earned it**. `cycleRole` and `cycleCache` are both keyed on `PROXY_URL` only, so a reader link pasted over a writer link in the same browser kept role `writer` until the first successful read; `bootstrapToken()` now drops both keys (never the queue) when the arriving token differs. `read-only` from `doPost` is the third refusal shape and the only one the read path can never see — a shared `forgetRole()` now fires from `postEntry()` as well as the GET handler. In `sw.js`: `activate` swept **every** cache on the origin, and github.io serves all of Mike's repos from one origin, so it could delete another site's shell (now prefix-scoped); and Chart.js sat inside the all-or-nothing `addAll`, so a bad minute at jsdelivr would cost the whole offline shell — it is now a best-effort `add`, which the page already copes with via `chartsUnavailable()`. Plus: `register()` resolving is not evidence the install succeeded, so the page now watches the installing worker for `redundant`. 15 checks added across `offline-selfcheck` and `render-selfcheck`, 8 mutations red. All 8 self-checks PASS. **Live on `6f03eba` and passed by Mike 2026-09-15**, writer and reader both — the reader half run as a link swap on the phone that had held the writer link since ca9, which is finding 1's own scenario rather than a stub of it.
- **ca10** — the flaky second hop. A write that landed was reported as failed (Sep 15, live). `postEntry()` split into `postOnce()` (one fetch; a 404/5xx now flagged with `hopStatus` rather than swallowed as an unreadable reply) and a retry loop around it: 4 attempts and `attempt * 1000` backoff, the same rule as `verify-proxy.js`, with each file's comment pointing at the other. A **reply** returns immediately, so a refusal is still final on the first answer and ca9a's `forgetRole()` stays outside the retry. Retried: timeout, 404/5xx, and a `TypeError` while `navigator.onLine` is not `false`; all three also mean the write **may have landed**. The stop is a wall-clock deadline (save 100s, flush 180s) checked before an attempt starts, because a timeout costs 45s and a 404 costs a moment; per-attempt timeout 30s—45s, matching `verify-proxy`. `saveEntry` shows "trying again (attempt N)" on the spinner, `flushQueue` uses the console. Second half: an entry carries `unknown`, and the banner/notes say "has not been confirmed by the sheet" and "sending it again is safe" instead of claiming it never arrived; `e.tries` is finally on screen from two sends up. Found on the way: `budgetMs || DEFAULT` silently gave a zero budget the full two minutes. 26 checks added to `queue-selfcheck.js`, **13 mutations red**, and the file now fails if the process exits 0 without reaching its last line. All 8 self-checks PASS. **Live test is Mike's** — the honest exit is a retry visible in the console during normal use, plus `verify.bat`.

## Open deviations from spec
_(Things later slices must know. One line each. Delete once resolved.)_
- **`ok:true`, a timeout and now a refusal are all weak evidence** (ca6, ca10, ca11) — `ok:true` means the request was accepted, not that the sheet holds what was sent; a timeout means nothing came back, **not** that the write failed. The app now says only what it observed (`writeFailureKind`/`writeAttemptText`), and the retry rule is shared with `verify-proxy.js` — keep them in step or say why in both files. **A `no-access` is not proof either** (ca11, proven 2026-09-15): Apps Script runs the write on the first hop and 302s to a one-time answer URL, and a second fetch of that URL runs `doGet` with no token — so a landed write can come back refused. Until ca11 lands, a refusal from `doPost` may be a phantom.
- **A revoked sheet permission reads as staleness** — the proxy answers `read-failed:`, so the viewer gets a dated cached dashboard, not an error. Correct for Tirzah; a banner is not proof of a token problem.
- **Two known limits in how `writeRow` reads the sheet** (ca6a) — a Note cell that already holds a formula is normalised, not recovered (`getValues()` turns `=1+1` into `2` and RAW stores the text `"2"`; ca5a stops new ones, and a `getFormulas()` read on every write is not worth it); and `unreadableDates` under-reports, because the date scan `break`s on the matched row. Both only have to send you to look at the sheet.
- **`page-harness.js` answers more than a real DOM** (ca9a) — `getElementById()` never returns null and `parentNode` always resolves, so a check can go green on a lookup that would be null on the phone. No live defect today (`chartsUnavailable()` only runs once the canvases are drawn), but it is the shape of the false greens this plan keeps finding.

## Locked decisions
_(From the /grill-me passes. Do not re-litigate. Slice-specific decisions live in their own slice files.)_
### Scope
- **The lunation wheel is scrapped** (2026-09-13, Tirzah didn't like it). No circular view, no lunation framing; its safety and display rules survive in ca4, its geometry does not.
- **The dashboard is the app** — not restyled, not rebuilt. No visual redesign in this plan.
- **The moon stays where it is**: the glyph strip above the timeline chart and the Moon at Day 1 / Moon at
  Ovulation columns. Nothing else lunar gets built.
- **Tirzah is hands off** — no design gate. In exchange, surfaces she sees change **only** for correctness.
### Data and access
- The sheet is the **single source of truth**. Local storage holds only unsent entries plus a display cache.
- **No pre-created future rows.** One row per actually-logged day — blanks caused the ca1 `detectPhase` bug.
- **A token the browser would not save stays in the address bar.** The `#t=` scrub is skipped when the write
  fails, and the self-reload carries the fragment. Mike ruled the screenshot leak acceptable (2026-09-15): an app
  that cannot be reopened is worse. Do not "tidy up" the scrub into an unconditional one.
- The **old sheet ID is permanently burned** (public repo since the first commit). New sheet, new ID, mandatory.
- The **original spreadsheet is kept untouched** as a historical keepsake.
- The repo **stays public** on GitHub Pages. All security lives in the two tokens, not in repo privacy.
- **Two tokens:** reader (Tirzah) and writer (Mike); the writer token also grants read — one link per person. A
  bad or missing token must produce a **visible message**, never a blank screen.
- Target browser is **Chrome on Android (Galaxy S22 Ultra)** only. No Safari/ITP workarounds.
- **Cervix Position is never inferred from texture** — it double-counts one observation in ca4.
### Shape
- **Same app both sides, one URL.** Reader gets one screen, **no tab bar**; writer gets `Dashboard | Log`. Hiding
  the tab is cosmetic — **writes are enforced in the Apps Script**, never the client.
- Writer-only surfaces: the Log tab and the unsent-queue banner. Viewer-only: staleness banner and 3-day expiry.
### Rules
- Ovulation day is a **manual marker only**. No calculation may ever declare an ovulation day.
- **No ovulation marker => no coverline, and the safe window never opens.** Three-over-six may only *delay* the
  opening, never trigger it. (2026-08-26: without this it declared a false "Safe" on cycle-5 day 12.)
- Cycle day is **date arithmetic** from the Cycle Start flag — never a lookup of the last logged row.
- Two safety states only: **Safe / Unsafe**. "Unsafe" absorbs "unknown".
- Status vocabulary is **Bleeding / Follicular / Luteal** only. **Ovulation is never a status.**
- **Mike drives all browser verification.** Never launch a browser tool to verify a change.
